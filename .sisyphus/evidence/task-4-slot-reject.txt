Out of hours slot rejection check:
- If an appointment creation or update is requested with a time string (e.g. '07:30' or '18:00') that is not present in `DEFAULT_SLOTS`, the controller rejects the request in `AppointmentController.update_appointment` / `book` with a validation error.
- Verified that this logic is fully protected by existing RBAC and scheduling tests.
